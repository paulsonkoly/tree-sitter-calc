{
  "name": "tree-sitter-calc",
  "version": "1.0.0",
  "description": "calc language tree-sitter parser",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paul Sonkoly",
  "license": "MIT",
  "devDependencies": {
    "tree-sitter-cli": "^0.21.0"
  }
}
